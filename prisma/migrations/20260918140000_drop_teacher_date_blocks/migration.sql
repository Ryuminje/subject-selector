-- 날짜별 교체 불가 표시를 걷어냅니다. "한쪽 교사를 그 날 빼둔다"는 메모로는 실제로 무슨
-- 교체가 있었는지 알 수 없어, 이미 이뤄진 교체·보강을 그대로 입력하는 방식으로 바꿉니다.
-- 앞선 마이그레이션을 지우면 이미 적용한 DB와 이력이 어긋나므로 컬럼을 떨어뜨립니다.

-- AlterTable
ALTER TABLE "Teacher" DROP COLUMN "dateBlocks";
